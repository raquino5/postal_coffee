<!-- app/views/shared/_product_sidebar.html.erb -->

<div class="card sidebar-card">
  <h3 class="mb-2">Shopping</h3>

  <ul class="sidebar-list">
    <li>
      <%= link_to cart_path, class: "sidebar-link d-flex align-items-center gap-2" do %>
        🛒 <span>View Cart (<%= cart_count %>)</span>
      <% end %>
    </li>
  </ul>
</div>

<div class="card sidebar-card">
  <h3 class="mb-2">Categories</h3>

  <ul class="sidebar-list">
    <li>
      <%= link_to products_path,
            class: "sidebar-link d-flex align-items-center gap-2" do %>
        📦 <span>All Products</span>
      <% end %>
    </li>

    <% categories.each do |category| %>
      <li>
        <%= link_to category_path(category),
              class: "sidebar-link d-flex align-items-center gap-2" do %>
          ● <span><%= category.name %></span>
        <% end %>
      </li>
    <% end %>
  </ul>
</div>
